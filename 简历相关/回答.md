1.基于langgraph的封装，原因是因为这个日志分析流程，并不是简单的一套流程的集合。它涉及到了复杂的中间状态，和多轮状态流转loop。
reason 模块根据之前提供的skill，tool，进行function calling，act前置节点，进行schema校验和权限拦截filter，通过后调用tool，tool结果发生后，将结果传递给observe模块，llm生成结果，并同步agent state
2.这个yaml配置，实际上是根据domain决定的。新增加一个领域的domain时，需要指定激活的skill，tool，排查问题的参考prompt，代码仓库属性
新增业务领域，skill/tool零改动，只需要动态配置domain生效的桉树即可
3.重复检测没有用相似度算法，这里是对tool调用的。

全历史中 (tool, arguments) 出现 ≥2 次（非连续） │ 软提示 
连续同工具 │ 最近 4 次调用全部是同一 tool（len(set)==1）     │ 软提示 
每过6轮，自动归纳之前的工具调用记录结果  │ 软提示 
max_turns 循环上限（硬）+ 连续 3次失败  熔断（硬，workflow.py:668-691）。


4.是errorcode + message，同个工具的错误信息只保留最近一个在上下文中，并且每次调用都会有call id，区分调用次数
  - 触发：tool_result 的 data JSON > 2048B 且不在最近 keep_recent（decide 用 3、prompt_input 用 4）条内；
  - 动作：删掉 data，换成 data_summary（截 600 字符摘要），保留 `ok/error_code/tool_name/call_id` 等小字段（trace.py:122-140）；
  - 只在「组装 prompt 给 LLM 之前」生效，不写回 context["history"] 原数据——历史真相源不动。

5.白名单，就是之前说的不同领域的yaml配置，里面会指定激活的skill和tool，构建system prompt时会以此为基准构造。这是第一层fillter。模型进行tool call时，还会再校验一次，tool是否激活，schema是否正确。权限拒绝的结构化回灌"，就是返回权限不允许，尝试其他工具
agent 拿到 Shell 工具，你靠什么保证它不会逃逸出白名单：
grep，tail， find 等命令，加入了白名单
rm,-rf,sudo，直接正则否决
读写操作，path只允许在工作区内，才能执行通过

6.bm25用的是sqllite的 fts bm25库，rerank用的是qwen-rank模型。这里先进行意图识别，简单问题直接回复，复杂问题，进行rewrite，拆分子项，再去embedding 做rag检索。检索结果 按照 向量+关键词+以及经验质量综合打分，取top 2
"日志检索"和"代码仓库检索"，都是shell grep，没做召回。后期考虑代码仓库，按照版本，对核心逻辑做召回，增加检索速度和准确率
7.check point里存储了，会话history，agent state（包含已经收敛的结论，执行次数，domain类型，观测的事实依据，当前假设）
8.平均响应时间，最近30天数据是4小时；75指的是，问题描述清晰，日志未过期，有明确的现象，93%规定轮次内正常输出了结论，并且人工check后，80%可以采纳


